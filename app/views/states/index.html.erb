<h1>States</h1>

<%= link_to 'New State', new_state_path, class: "small button" %>

<ul>
  <% @states.each do |state| %>
    <li><%= link_to state.name, state %>
      (<%= pluralize state.counties.count, "county" %>)
    </li>
  <% end %>
</ul>

