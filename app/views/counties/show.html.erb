<h1><%= @county.name %></h1>

<p>
  <strong>State:</strong>
  <%= link_to @county.state.name, @county.state %>
</p>

<p>
  <strong>Search url:</strong>
  <%= @county.search_url %>
</p>

<p>
  <strong>Average days to complete:</strong>
  <%= @county.average_days_to_complete %>
</p>

<%= link_to 'Edit', edit_state_county_path(@state, @county) %> |
<%= link_to 'Delete', [@state, @county], method: :delete, data: { confirm: 'Are you sure?' } %>
